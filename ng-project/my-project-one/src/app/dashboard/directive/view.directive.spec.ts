import { ViewDirective } from './view.directive';

describe('ViewDirective', () => {
  it('should create an instance', () => {
    const directive = ViewDirective;
    expect(directive).toBeTruthy();
  });
});
