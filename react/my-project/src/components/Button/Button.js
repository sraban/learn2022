import React from 'react';
import PropTypes from 'prop-types';
import './Button.scss';

const Button = () => (
  <div className="Button">
    Button Component
  </div>
);

Button.propTypes = {};

Button.defaultProps = {};

export default Button;
