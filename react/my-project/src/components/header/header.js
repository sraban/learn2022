import React from 'react';
import PropTypes from 'prop-types';
import './header.scss';

const Header = () => (
  <div className="header">
    Header Component
  </div>
);

Header.propTypes = {};

Header.defaultProps = {};

export default Header;
